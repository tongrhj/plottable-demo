{
  "name": "plottable-demo",
  "version": "1.0.0",
  "description": "Demo Graph of Plottable",
  "main": "index.html",
  "scripts": {
    "start": "npm run build && http-server",
    "watch": "npm run build && live-server",
    "build": "babel src -d dist",
    "deploy": "surge --project ./ --domain plottable.surge.sh",
    "test": "standard"
  },
  "author": "Jared Tong",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.6.5",
    "babel-preset-es2015": "^6.6.0",
    "http-server": "^0.9.0",
    "live-server": "^0.9.2",
    "standard": "^6.0.8",
    "surge": "^0.17.7"
  }
}
